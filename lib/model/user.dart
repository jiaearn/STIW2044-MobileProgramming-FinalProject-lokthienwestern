class User {
  String username, email, fullname, regDate, gender, contact;
  User(
      {this.username,
      this.email,
      this.fullname,
      this.regDate,
      this.gender,
      this.contact});
}
